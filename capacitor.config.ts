import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lighten.asgadasga',
  appName: 'asgadasga',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
